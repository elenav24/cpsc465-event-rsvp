# RDS has been removed — database migrated to Neon (https://neon.tech).
# The aws_db_instance.postgres resource has been deleted.
# Run `terraform state rm aws_db_instance.postgres` after deleting the RDS
# instance in the AWS console to clean up the Terraform state.
